(function () {
    'use strict';

    angular.module('my-app', [
            // dependencies
        ])
        .config(config)
        .run(run);

    config.$inject = [
        // provider dependencies
    ];

    run.$inject = [
        // instance dependencies
    ];

    function config() {

    }

    function run() {

    }
})();